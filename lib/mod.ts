import capture from "./core/capture.ts";
import recap from "./core/recap.ts";
import fingerprint from "./core/fingerprint.ts";
import CaptureClient from "./core/client.ts";

export default CaptureClient;

export { capture, fingerprint, recap };
